use actix_web::web;

pub fn routes(cfg: &mut web::ServiceConfig) {
    // cfg.service(
    //     web::scope("/api").service(web::scope("/article").route("/list", web::get().to(wx_login))),
    // );
}
